// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyD8sJIOymCYRVykFm5W7cduDJbIsKPrnVY",
    authDomain: "character-detailer.firebaseapp.com",
    databaseURL: "https://character-detailer.firebaseio.com",
    projectId: "character-detailer",
    storageBucket: "character-detailer.appspot.com",
    messagingSenderId: "710538621182",
    appId: "1:710538621182:web:4d41c26029c90609fc75af"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
